Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
let node_child_process = require("node:child_process");
let js_jsonl = require("js-jsonl");
let node_path = require("node:path");
let node_url = require("node:url");
let node_fs = require("node:fs");
let node_fs_promises = require("node:fs/promises");
let node_os = require("node:os");
//#region src/agents/types.ts
function emptyTokenUsage() {
	return {
		inputTokens: null,
		outputTokens: null,
		cacheReadTokens: null,
		cacheWriteTokens: null
	};
}
function readTokenCount(value) {
	return typeof value === "number" && Number.isFinite(value) ? value : null;
}
//#endregion
//#region src/agents/docker.ts
const runDocker = (args, options = {}) => new Promise((resolve, reject) => {
	const child = (0, node_child_process.spawn)("docker", args, {
		stdio: [
			"ignore",
			"pipe",
			"pipe"
		],
		env: {
			...process.env,
			...options.env
		}
	});
	let stdout = "";
	let stderr = "";
	const inheritOutput = options.inheritOutput === true;
	child.stdout.on("data", (chunk) => {
		const text = chunk.toString();
		stdout += text;
		if (inheritOutput) process.stdout.write(text);
	});
	child.stderr.on("data", (chunk) => {
		const text = chunk.toString();
		stderr += text;
		if (inheritOutput) process.stderr.write(text);
	});
	child.on("error", (error) => {
		reject(/* @__PURE__ */ new Error(`Failed to start docker: ${error.message}. Is Docker installed and running?`));
	});
	child.on("close", (exitCode) => {
		resolve({
			exitCode,
			stdout,
			stderr
		});
	});
});
function buildDockerRunArgs(options) {
	const args = [
		"run",
		"--rm",
		"--user",
		`${options.uid}:${options.gid}`
	];
	if (options.env !== void 0) for (const [key, value] of Object.entries(options.env)) args.push("-e", `${key}=${value}`);
	if (options.envPassthrough !== void 0) for (const name of options.envPassthrough) args.push("-e", name);
	if (options.volumes !== void 0) for (const volume of options.volumes) {
		const modeSuffix = volume.mode === "ro" ? ":ro" : "";
		args.push("-v", `${volume.host}:${volume.container}${modeSuffix}`);
	}
	args.push("-w", options.workdir, options.image, ...options.command);
	return args;
}
async function invokeDocker(options, dockerRunner = runDocker) {
	return dockerRunner(buildDockerRunArgs(options));
}
//#endregion
//#region src/agents/ensure-image.ts
async function ensureDockerImage(image, options = {}) {
	if ((await (options.dockerRunner ?? runDocker)([
		"image",
		"inspect",
		image
	])).exitCode === 0) return;
	throw new Error(`Docker image ${image} not found. Call buildImages() from vitest globalSetup (or build the image manually) before running agent tests.`);
}
//#endregion
//#region src/agents/trajectory/values.ts
function asRecord(value) {
	if (typeof value !== "object" || value === null) return;
	return value;
}
function readString(value) {
	return typeof value === "string" ? value : null;
}
function extractTextContent(message) {
	const content = asRecord(message)?.content;
	if (typeof content === "string") return content;
	if (!Array.isArray(content)) return "";
	const parts = [];
	for (const block of content) {
		const item = asRecord(block);
		if (item?.type === "text" && typeof item.text === "string") parts.push(item.text);
	}
	return parts.join("");
}
//#endregion
//#region src/agents/parse-result.ts
/**
* Parse agent CLI output (single JSON object or JSONL stream) and return the
* terminal `result` event when present.
*/
function parseAgentJsonOutput(text) {
	const trimmed = text.trim();
	if (trimmed === "") throw new Error("Agent produced empty trajectory; expected JSON output");
	let events;
	try {
		events = js_jsonl.jsonl.parse(trimmed);
	} catch (error) {
		const detail = error instanceof Error ? error.message : String(error);
		throw new Error(`Agent trajectory was not valid JSON: ${detail}\nTrajectory:\n${text}`);
	}
	let lastResult;
	for (const event of events) if (asRecord(event)?.type === "result") lastResult = event;
	if (lastResult !== void 0) return lastResult;
	if (events.length === 1) return events[0];
	throw new Error("Agent trajectory has no terminal result event");
}
//#endregion
//#region src/package-root.ts
/** Absolute path to the clanker-cleanroom package root (parent of `src/` or `lib/`). */
const PACKAGE_ROOT = (0, node_url.fileURLToPath)(new URL("..", require("url").pathToFileURL(__filename).href));
//#endregion
//#region src/images/graph.ts
/**
* Topological sort of Dockerfile entries by local FROM dependencies.
* Throws if a cycle is detected.
*/
function topoSort(entries) {
	const byTag = new Map(entries.map((entry) => [entry.tag, entry]));
	const visiting = /* @__PURE__ */ new Set();
	const visited = /* @__PURE__ */ new Set();
	const ordered = [];
	function visit(tag, path) {
		if (visited.has(tag)) return;
		if (visiting.has(tag)) throw new Error(`Dockerfile dependency cycle detected: ${[...path, tag].join(" -> ")}`);
		visiting.add(tag);
		const entry = byTag.get(tag);
		if (entry === void 0) throw new Error(`Unknown Dockerfile tag "${tag}"`);
		for (const dep of entry.dependencies) visit(dep, [...path, tag]);
		visiting.delete(tag);
		visited.add(tag);
		ordered.push(entry);
	}
	for (const entry of entries) visit(entry.tag, []);
	return ordered;
}
//#endregion
//#region src/agents/base/constants.ts
const BASE_IMAGE = "clanker-cleanroom/base";
/** Home directory inside every agent image; world-writable so any host uid can use it. */
const CONTAINER_HOME = "/home/agent";
/** Bind-mount target for the test workspace inside every agent image. */
const CONTAINER_WORKSPACE = "/workspace";
/** Read-only bind-mount for staged agent input files. */
const CONTAINER_INPUT = "/agent/input";
/** Read-write bind-mount for agent output files. */
const CONTAINER_OUTPUT = "/agent/output";
//#endregion
//#region src/agents/binding-registry.ts
/** Registered stock bindings keyed by short name (e.g. `"cursor"`). */
const bindingRegistry = {};
/** Register a stock agent binding for name and image-tag lookup. */
function registerBinding(name, binding) {
	bindingRegistry[name] = binding;
}
/** Remove a previously registered binding. */
function unregisterBinding(name) {
	delete bindingRegistry[name];
}
/** Clear all registered bindings (tests). */
function resetBindings() {
	for (const key of Object.keys(bindingRegistry)) delete bindingRegistry[key];
}
function resolveBinding(name) {
	const binding = bindingRegistry[name];
	if (binding === void 0) throw new Error(`No binding registered for "${name}". Import \`@clanker-cleanroom/${name}/register\` (or call registerBinding) before constructing Agent("${name}").`);
	return binding;
}
//#endregion
//#region src/agents/stock.ts
/** Image tags for currently registered stock agents. */
function stockAgentImages() {
	return Object.fromEntries(Object.entries(bindingRegistry).map(([name, binding]) => [name, binding.image]));
}
/** Map a Docker image tag to a registered stock agent name, if any. */
function stockAgentNameForImage(image) {
	for (const [name, binding] of Object.entries(bindingRegistry)) if (binding.image === image) return name;
}
function isStockAgentName(name) {
	return Object.hasOwn(bindingRegistry, name);
}
//#endregion
//#region src/images/infer-agent.ts
/**
* Infer which stock agent binding an image should use by walking FROM images.
* Returns undefined for non-agent images (e.g. base).
*/
function inferAgent(entry, localByTag, registry, visiting = /* @__PURE__ */ new Set()) {
	const stockForTag = stockAgentNameForImage(entry.tag);
	if (stockForTag !== void 0) return stockForTag;
	if (entry.tag === "clanker-cleanroom/base") return;
	if (visiting.has(entry.tag)) throw new Error(`Cycle while inferring agent for "${entry.tag}"`);
	visiting.add(entry.tag);
	try {
		for (const from of entry.fromImages) {
			const stock = stockAgentNameForImage(from);
			if (stock !== void 0) return stock;
			if (from === "clanker-cleanroom/base") continue;
			const local = localByTag.get(from);
			if (local !== void 0) {
				const inferred = inferAgent(local, localByTag, registry, visiting);
				if (inferred !== void 0) return inferred;
				continue;
			}
			const recorded = registry.images[from]?.agent;
			if (recorded !== void 0) return recorded;
		}
		if (!entry.fromImages.some((from) => from !== "clanker-cleanroom/base" && (localByTag.has(from) || registry.images[from] !== void 0))) return;
		throw new Error(`Cannot determine stock agent for image "${entry.tag}". FROM chain must reach a registered stock agent image (import \`@clanker-cleanroom/<agent>/register\` before buildImages).`);
	} finally {
		visiting.delete(entry.tag);
	}
}
//#endregion
//#region src/images/parse.ts
const TAG_COMMENT = /^#\s*(\S+)\s*$/;
const FROM_LINE = /^FROM\s+(?:--platform=\S+\s+)?([^\s]+)(?:\s+AS\s+\S+)?\s*$/i;
/**
* Discover `*.Dockerfile` files in `dir` and parse first-line tags + FROM images.
*/
function parseDockerfiles(dir) {
	const names = (0, node_fs.readdirSync)(dir).filter((name) => name.endsWith(".Dockerfile")).sort();
	if (names.length === 0) throw new Error(`No *.Dockerfile files found in ${dir}`);
	const entries = [];
	const tags = /* @__PURE__ */ new Set();
	for (const name of names) {
		const file = (0, node_path.join)(dir, name);
		const lines = (0, node_fs.readFileSync)(file, "utf8").split(/\r?\n/);
		const first = lines[0] ?? "";
		const tagMatch = TAG_COMMENT.exec(first);
		if (tagMatch === null) throw new Error(`Dockerfile ${name} must start with a tag comment "# <image>" (e.g. "# clanker-cleanroom/cursor"). Got: ${JSON.stringify(first)}`);
		const tag = tagMatch[1];
		if (tags.has(tag)) throw new Error(`Duplicate Dockerfile tag "${tag}" in ${dir}`);
		tags.add(tag);
		const fromImages = [];
		for (const line of lines) {
			const trimmed = line.trim();
			const fromMatch = FROM_LINE.exec(trimmed);
			if (fromMatch !== null) fromImages.push(fromMatch[1]);
		}
		entries.push({
			file,
			relative: name,
			tag,
			fromImages,
			dependencies: []
		});
	}
	const tagSet = new Set(entries.map((entry) => entry.tag));
	for (const entry of entries) entry.dependencies = entry.fromImages.filter((image) => tagSet.has(image));
	return entries;
}
//#endregion
//#region src/images/registry.ts
const IMAGES_REGISTRY_FILENAME = "clanker-cleanroom.images.json";
const IMAGE_REGISTRY_VERSION = 2;
function registryPath(options = {}) {
	return (0, node_path.join)((0, node_path.resolve)(options.packageRoot ?? process.cwd()), IMAGES_REGISTRY_FILENAME);
}
function emptyRegistry() {
	return {
		version: 2,
		images: {}
	};
}
function readRegistry(options = {}) {
	const file = registryPath(options);
	if (!(0, node_fs.existsSync)(file)) return emptyRegistry();
	const parsed = JSON.parse((0, node_fs.readFileSync)(file, "utf8"));
	if (parsed.version !== 2) throw new Error(`Image registry at ${file} has version ${String(parsed.version)}; expected 2. Delete it and run buildImages() again.`);
	if (typeof parsed.images !== "object" || parsed.images === null) throw new Error(`Invalid image registry at ${file}`);
	return parsed;
}
function writeRegistry(registry, options = {}) {
	const file = registryPath(options);
	(0, node_fs.mkdirSync)((0, node_path.dirname)(file), { recursive: true });
	const temp = `${file}.${process.pid}.tmp`;
	(0, node_fs.writeFileSync)(temp, `${JSON.stringify(registry, null, 2)}\n`);
	(0, node_fs.renameSync)(temp, file);
}
function upsertRegistryEntry(tag, entry, options = {}) {
	const registry = readRegistry(options);
	registry.images[tag] = entry;
	writeRegistry(registry, options);
}
function resolveImage(tag, options = {}) {
	return readRegistry(options).images[tag]?.image;
}
function resetRegistry(options = {}) {
	const file = registryPath(options);
	if ((0, node_fs.existsSync)(file)) (0, node_fs.writeFileSync)(file, `${JSON.stringify(emptyRegistry(), null, 2)}\n`);
}
//#endregion
//#region src/images/build.ts
const inFlight = /* @__PURE__ */ new Map();
function resetBuildMemo() {
	inFlight.clear();
}
/**
* Scan a Dockerfile folder, topo-sort by local FROM tags, build each image, and
* record tags in `clanker-cleanroom.images.json` at packageRoot (default cwd).
*/
async function buildImages(options = {}) {
	const dir = options.dir ?? (0, node_path.join)(PACKAGE_ROOT, "docker");
	const packageRoot = options.packageRoot ?? process.cwd();
	const force = options.force === true;
	const dockerRunner = options.dockerRunner ?? runDocker;
	const memoKey = `${dir}::${packageRoot}::${force}`;
	const existing = inFlight.get(memoKey);
	if (existing !== void 0) return existing;
	const pending = doBuild(dir, packageRoot, force, dockerRunner).catch((error) => {
		inFlight.delete(memoKey);
		throw error;
	});
	inFlight.set(memoKey, pending);
	return pending;
}
async function doBuild(dir, packageRoot, force, dockerRunner) {
	const entries = topoSort(parseDockerfiles(dir));
	const localByTag = new Map(entries.map((entry) => [entry.tag, entry]));
	let registry = readRegistry({ packageRoot });
	for (const entry of entries) {
		if (!force) {
			if (registry.images[entry.tag] !== void 0) {
				if ((await dockerRunner([
					"image",
					"inspect",
					entry.tag
				])).exitCode === 0) {
					process.stderr.write(`[clanker-cleanroom] Docker image ${entry.tag} already present\n`);
					continue;
				}
			}
		}
		process.stderr.write(`[clanker-cleanroom] Building Docker image ${entry.tag}...\n`);
		const build = await dockerRunner([
			"build",
			"--progress=plain",
			"-t",
			entry.tag,
			"-f",
			entry.file,
			dir
		], { inheritOutput: true });
		if (build.exitCode !== 0) throw new Error(`Failed to build image ${entry.tag}.\nstderr:\n${build.stderr}\nstdout:\n${build.stdout}`);
		process.stderr.write(`[clanker-cleanroom] Built Docker image ${entry.tag}\n`);
		registry = recordBuiltImage(entry, localByTag, registry, packageRoot);
	}
}
function recordBuiltImage(entry, localByTag, registry, packageRoot) {
	const agent = inferAgent(entry, localByTag, registry);
	upsertRegistryEntry(entry.tag, {
		image: entry.tag,
		dockerfile: entry.relative,
		builtAt: (/* @__PURE__ */ new Date()).toISOString(),
		...agent !== void 0 ? { agent } : {}
	}, { packageRoot });
	return readRegistry({ packageRoot });
}
//#endregion
//#region src/agents/agent-fs.ts
/**
* Host-backed file tree staged into the container at {@link AgentFs.containerPath}.
* Roots are created lazily under a temp directory (swap-friendly later via a provider).
*/
var AgentFs = class {
	containerPath;
	root;
	tmpPrefix;
	constructor(containerPath, options = {}) {
		this.containerPath = containerPath;
		this.tmpPrefix = (0, node_path.join)(options.tmpDir ?? (0, node_os.tmpdir)(), "clanker-io-");
	}
	/** Ensure the host root exists and return its absolute path (for Docker mounts). */
	async ensure() {
		if (this.root === void 0) this.root = await (0, node_fs_promises.mkdtemp)(this.tmpPrefix);
		return this.root;
	}
	async write(relativePath, data) {
		const absolutePath = resolveSafe(await this.ensure(), relativePath);
		await (0, node_fs_promises.mkdir)((0, node_path.dirname)(absolutePath), { recursive: true });
		await (0, node_fs_promises.writeFile)(absolutePath, data);
	}
	async read(relativePath) {
		const absolutePath = resolveSafe(await this.ensure(), relativePath);
		return (0, node_fs_promises.readFile)(absolutePath);
	}
	async readText(relativePath) {
		return (await this.read(relativePath)).toString("utf8");
	}
	/** Relative POSIX paths of all files under the root. */
	async list() {
		const root = await this.ensure();
		const names = await (0, node_fs_promises.readdir)(root, { recursive: true });
		const files = [];
		for (const name of names) {
			const absolutePath = (0, node_path.join)(root, name);
			if (!isInsideRoot(root, absolutePath)) throw new Error(`AgentFs: listed path escapes root: ${absolutePath}`);
			if (!(await (0, node_fs_promises.stat)(absolutePath)).isFile()) continue;
			files.push(toPosixRelative(root, absolutePath));
		}
		return files.sort();
	}
	/** Remove all contents; keeps (or creates) the empty root directory. */
	async clear() {
		const root = await this.ensure();
		const entries = await (0, node_fs_promises.readdir)(root);
		await Promise.all(entries.map((entry) => (0, node_fs_promises.rm)((0, node_path.join)(root, entry), {
			recursive: true,
			force: true
		})));
	}
};
function resolveSafe(root, relativePath) {
	assertSafeRelative(relativePath);
	const absolutePath = (0, node_path.resolve)(root, ...relativePath.replaceAll("\\", "/").split("/"));
	if (!isInsideRoot(root, absolutePath)) throw new Error(`AgentFs: path escapes root: ${relativePath}`);
	return absolutePath;
}
function assertSafeRelative(relativePath) {
	if (relativePath.includes("\0")) throw new Error(`AgentFs: path contains null byte: ${relativePath}`);
	const segments = relativePath.replaceAll("\\", "/").split("/");
	if (segments.length === 0 || segments.some((segment) => segment === "" || segment === "." || segment === "..")) throw new Error(`AgentFs: not a safe relative path: ${relativePath}`);
}
function toPosixRelative(root, absolutePath) {
	return (0, node_path.relative)(root, absolutePath).split(node_path.sep).join("/");
}
function isInsideRoot(root, absolutePath) {
	const rel = (0, node_path.relative)((0, node_path.resolve)(root), (0, node_path.resolve)(absolutePath));
	return rel !== "" && !rel.startsWith(`..${node_path.sep}`) && rel !== ".." && !rel.startsWith("../");
}
//#endregion
//#region src/agents/run-error.ts
/** Error for a non-zero `docker run` exit, with a build hint when docker could not find the image. */
function agentRunError(options) {
	const { exitCode, stdout, stderr } = options.result;
	const headline = options.detail === void 0 ? `${options.agent} agent exited with code ${exitCode}.` : `${options.agent} agent exited with code ${exitCode}: ${options.detail}`;
	const hint = stderr.includes("Unable to find image") || stderr.includes("not found") ? `\nDocker image ${options.image} not found; build it with buildImages() in vitest globalSetup.` : "";
	return /* @__PURE__ */ new Error(`${headline}${hint}\nstderr:\n${stderr}\nstdout:\n${stdout}`);
}
//#endregion
//#region src/agents/trajectory/constants.ts
/** Relative path under {@link AgentFs} / `/agent/output` for the streamed CLI NDJSON log. */
const TRAJECTORY_FILE = "trajectory";
//#endregion
//#region src/agents/trajectory/wrap-command.ts
/** Container path where the agent CLI streams NDJSON via shell redirect. */
const CONTAINER_TRAJECTORY_PATH = `${CONTAINER_OUTPUT}/${TRAJECTORY_FILE}`;
/**
* Wrap agent argv so stdout streams to {@link CONTAINER_TRAJECTORY_PATH}.
* Prompt and flags stay as separate argv entries (no shell quoting).
*/
function wrapCommandWithTrajectoryRedirect(command) {
	return [
		"sh",
		"-c",
		`exec "$@" > ${CONTAINER_TRAJECTORY_PATH}`,
		"sh",
		...command
	];
}
//#endregion
//#region src/agents/trajectory/parse.ts
function parseTrajectory(ndjson, provider, adaptEvents) {
	return createTrajectory(provider, adaptEvents(js_jsonl.jsonl.parse(ndjson)));
}
async function readTrajectory(output, provider, adaptEvents) {
	return parseTrajectory(await output.readText(TRAJECTORY_FILE), provider, adaptEvents);
}
function createTrajectory(provider, events) {
	return {
		provider,
		events,
		sessionId() {
			for (const event of events) if (event.kind === "system" && event.sessionId !== null) return event.sessionId;
			return null;
		},
		result() {
			for (let i = events.length - 1; i >= 0; i -= 1) {
				const event = events[i];
				if (event?.kind === "result") return event;
			}
			return null;
		},
		assistantMessages() {
			return events.filter((event) => {
				return event.kind === "assistant";
			}).map((event) => event.text);
		},
		toolCalls() {
			const byId = /* @__PURE__ */ new Map();
			for (const event of events) {
				if (event.kind !== "tool") continue;
				const existing = byId.get(event.callId);
				if (existing === void 0) {
					byId.set(event.callId, {
						callId: event.callId,
						name: event.name,
						args: event.args,
						result: event.result
					});
					continue;
				}
				byId.set(event.callId, {
					callId: event.callId,
					name: event.name === "tool_result" ? existing.name : event.name,
					args: event.args ?? existing.args,
					result: event.result ?? existing.result
				});
			}
			return [...byId.values()];
		}
	};
}
//#endregion
//#region src/agents/run-bound.ts
async function runBoundAgent(binding, options, dockerRunner = runDocker) {
	const uid = options.uid ?? process.getuid?.() ?? 0;
	const gid = options.gid ?? process.getgid?.() ?? 0;
	const prepared = await binding.prepare({ workspace: options.workspace });
	const volumes = [
		{
			host: options.workspace,
			container: CONTAINER_WORKSPACE
		},
		...options.ioVolumes ?? [],
		...prepared.volumes ?? []
	];
	const outputHost = hostPathForContainer(volumes, CONTAINER_OUTPUT);
	if (outputHost === void 0) throw new Error(`runBoundAgent requires an ioVolume mounted at ${CONTAINER_OUTPUT} to capture trajectory`);
	const env = prepared.env ?? {};
	const result = await dockerRunner(buildDockerRunArgs({
		image: options.image,
		uid,
		gid,
		workdir: CONTAINER_WORKSPACE,
		env: { HOME: CONTAINER_HOME },
		envPassthrough: Object.keys(env),
		volumes,
		command: wrapCommandWithTrajectoryRedirect(binding.command({
			prompt: options.prompt,
			...options.model !== void 0 ? { model: options.model } : {}
		}))
	}), { env });
	const trajectory = await readTrajectoryFile(outputHost);
	if (result.exitCode !== 0) throw agentRunError({
		agent: binding.displayName,
		image: options.image,
		result,
		detail: binding.describeFailure?.(trajectory)
	});
	return binding.parseResult(trajectory);
}
function hostPathForContainer(volumes, containerPath) {
	return volumes.find((volume) => volume.container === containerPath)?.host;
}
async function readTrajectoryFile(outputHost) {
	try {
		return await (0, node_fs_promises.readFile)((0, node_path.join)(outputHost, TRAJECTORY_FILE), "utf8");
	} catch (error) {
		if (isNotFound(error)) return "";
		throw error;
	}
}
function isNotFound(error) {
	return typeof error === "object" && error !== null && "code" in error && error.code === "ENOENT";
}
//#endregion
//#region src/agents/agent.ts
/**
* Resolve a stock short name (`cursor`, `claude`) or a registry image tag
* (`cursor:node`) and run it with the matching binding.
*/
var Agent = class Agent {
	name;
	image;
	/** Host-staged files mounted read-only at `/agent/input`. */
	input;
	/** Host-staged files mounted read-write at `/agent/output`. */
	output;
	binding;
	registryOptions;
	constructor(nameOrBinding, options = {}) {
		this.input = new AgentFs(CONTAINER_INPUT);
		this.output = new AgentFs(CONTAINER_OUTPUT);
		if (typeof nameOrBinding !== "string") {
			const binding = nameOrBinding.__fromBinding;
			this.name = binding.image;
			this.image = binding.image;
			this.binding = binding;
			this.registryOptions = {};
			return;
		}
		const resolved = lookupAgent(nameOrBinding, options);
		this.name = nameOrBinding;
		this.image = resolved.image;
		this.binding = resolved.binding;
		this.registryOptions = options;
	}
	/** Wrap a custom binding that is not registered by name. */
	static fromBinding(binding) {
		return new Agent({ __fromBinding: binding });
	}
	async ensureImage() {
		await ensureDockerImage(this.image);
	}
	async buildImage(options) {
		await buildImages({
			...this.registryOptions,
			...options
		});
	}
	async run(options) {
		const inputHost = await this.input.ensure();
		const outputHost = await this.output.ensure();
		await this.output.clear();
		return runBoundAgent(this.binding, {
			workspace: options.workspace,
			prompt: options.prompt,
			image: options.image ?? this.image,
			...options.model !== void 0 ? { model: options.model } : {},
			ioVolumes: [{
				host: inputHost,
				container: this.input.containerPath,
				mode: "ro"
			}, {
				host: outputHost,
				container: this.output.containerPath
			}]
		});
	}
	/** Load and normalize the NDJSON trajectory written by the last {@link run}. */
	async trajectory() {
		return readTrajectory(this.output, this.binding.trajectoryKind, this.binding.adaptEvents);
	}
};
function lookupAgent(name, options) {
	if (isStockAgentName(name)) {
		const binding = resolveBinding(name);
		return {
			image: binding.image,
			binding
		};
	}
	const entry = readRegistry(options).images[name];
	if (entry === void 0) throw new Error(`Unknown agent "${name}". Import \`@clanker-cleanroom/<agent>/register\` for a stock name, or use a tag recorded in clanker-cleanroom.images.json via buildImages().`);
	if (entry.agent === void 0) throw new Error(`Image "${name}" is not a runnable agent (no stock agent in its FROM chain).`);
	const binding = {
		...resolveBinding(entry.agent),
		image: entry.image
	};
	return {
		image: entry.image,
		binding
	};
}
//#endregion
//#region src/agents/create-agent.ts
/** Wrap a custom binding that is not registered by name. */
function createAgent(binding) {
	return Agent.fromBinding(binding);
}
//#endregion
exports.Agent = Agent;
exports.AgentFs = AgentFs;
exports.BASE_IMAGE = BASE_IMAGE;
exports.CONTAINER_HOME = CONTAINER_HOME;
exports.CONTAINER_INPUT = CONTAINER_INPUT;
exports.CONTAINER_OUTPUT = CONTAINER_OUTPUT;
exports.CONTAINER_TRAJECTORY_PATH = CONTAINER_TRAJECTORY_PATH;
exports.CONTAINER_WORKSPACE = CONTAINER_WORKSPACE;
exports.IMAGES_REGISTRY_FILENAME = IMAGES_REGISTRY_FILENAME;
exports.IMAGE_REGISTRY_VERSION = IMAGE_REGISTRY_VERSION;
exports.PACKAGE_ROOT = PACKAGE_ROOT;
exports.TRAJECTORY_FILE = TRAJECTORY_FILE;
exports.asRecord = asRecord;
exports.bindingRegistry = bindingRegistry;
exports.buildDockerRunArgs = buildDockerRunArgs;
exports.buildImages = buildImages;
exports.createAgent = createAgent;
exports.emptyTokenUsage = emptyTokenUsage;
exports.ensureDockerImage = ensureDockerImage;
exports.extractTextContent = extractTextContent;
exports.invokeDocker = invokeDocker;
exports.isStockAgentName = isStockAgentName;
exports.parseAgentJsonOutput = parseAgentJsonOutput;
exports.parseDockerfiles = parseDockerfiles;
exports.parseTrajectory = parseTrajectory;
exports.readRegistry = readRegistry;
exports.readString = readString;
exports.readTokenCount = readTokenCount;
exports.readTrajectory = readTrajectory;
exports.registerBinding = registerBinding;
exports.registryPath = registryPath;
exports.resetBindings = resetBindings;
exports.resetBuildMemo = resetBuildMemo;
exports.resetRegistry = resetRegistry;
exports.resolveBinding = resolveBinding;
exports.resolveImage = resolveImage;
exports.runBoundAgent = runBoundAgent;
exports.runDocker = runDocker;
exports.stockAgentImages = stockAgentImages;
exports.stockAgentNameForImage = stockAgentNameForImage;
exports.topoSort = topoSort;
exports.unregisterBinding = unregisterBinding;
exports.upsertRegistryEntry = upsertRegistryEntry;
exports.wrapCommandWithTrajectoryRedirect = wrapCommandWithTrajectoryRedirect;

//# sourceMappingURL=index.cjs.map