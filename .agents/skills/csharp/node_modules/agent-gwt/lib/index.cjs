Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
let node_fs_promises = require("node:fs/promises");
let node_os = require("node:os");
let node_path = require("node:path");
let node_url = require("node:url");
let clanker_cleanroom = require("clanker-cleanroom");
//#region src/given/a_workspace.ts
const AGENTS_GWT_TMP_ROOT = (0, node_path.join)((0, node_os.tmpdir)(), ".agents-gwt");
async function a_workspace() {
	await (0, node_fs_promises.mkdir)(AGENTS_GWT_TMP_ROOT, { recursive: true });
	this.workspace = await (0, node_fs_promises.mkdtemp)((0, node_path.join)(AGENTS_GWT_TMP_ROOT, "ws-"));
}
async function cleanup_workspace() {
	if (this.workspace === void 0 || this.workspace === "") return;
	await (0, node_fs_promises.rm)(this.workspace, {
		recursive: true,
		force: true
	});
}
//#endregion
//#region src/given/copy_to_workspace.ts
async function copy_to_workspace(workspace, globs, options) {
	if (globs.length === 0) throw new Error("copy_to_workspace: globs must not be empty");
	const from = await resolveSourceRoot(options);
	const resolvedWorkspace = (0, node_path.resolve)(workspace);
	const base = options?.base;
	const copies = [];
	const destinationSources = /* @__PURE__ */ new Map();
	for (const glob of globs) {
		const matches = await expandGlob(from, glob);
		if (matches.length === 0) throw new Error(`copy_to_workspace: glob "${glob}" matched no files under ${from}`);
		for (const relativePath of matches) {
			const destinationRelative = base === void 0 ? relativePath : stripBase(relativePath, base);
			assertSafeRelative(destinationRelative, "destination");
			const source = (0, node_path.resolve)(from, relativePath);
			const destination = (0, node_path.resolve)(resolvedWorkspace, destinationRelative);
			if (!isInsideRoot(resolvedWorkspace, destination)) throw new Error(`copy_to_workspace: destination escapes workspace: ${destinationRelative}`);
			const existingSource = destinationSources.get(destinationRelative);
			if (existingSource !== void 0) {
				if (existingSource === source) continue;
				throw new Error(`copy_to_workspace: destination "${destinationRelative}" maps to multiple files`);
			}
			destinationSources.set(destinationRelative, source);
			copies.push({
				source,
				destination
			});
		}
	}
	for (const { source, destination } of copies) {
		await (0, node_fs_promises.mkdir)((0, node_path.dirname)(destination), { recursive: true });
		await (0, node_fs_promises.copyFile)(source, destination);
	}
}
async function resolveSourceRoot(options) {
	const from = options?.from;
	if (from !== void 0 && (0, node_path.isAbsolute)(from)) return (0, node_path.resolve)(from);
	const specDir = await currentSpecDirectory();
	if (from === void 0) return specDir;
	return (0, node_path.resolve)(specDir, from);
}
async function currentSpecDirectory() {
	const { expect } = await import("vitest");
	const testPath = expect.getState().testPath;
	if (testPath === void 0 || testPath === "") throw new Error("copy_to_workspace: cannot resolve the spec directory; pass options.from or call from a Vitest test");
	const specFile = testPath.startsWith("file:") ? (0, node_url.fileURLToPath)(testPath) : testPath;
	return (0, node_path.dirname)(specFile);
}
async function expandGlob(from, glob) {
	const posixGlob = glob.replaceAll("\\", "/");
	const matcher = globToRegExp(posixGlob);
	const prefixSegments = globStaticPrefix(posixGlob);
	const walkRoot = prefixSegments.length === 0 ? from : (0, node_path.join)(from, ...prefixSegments);
	if (!isInsideRootOrEqual(from, walkRoot)) throw new Error(`copy_to_workspace: glob "${glob}" escapes source root`);
	let walkInfo;
	try {
		walkInfo = await (0, node_fs_promises.stat)(walkRoot);
	} catch (error) {
		if (isEnoent(error)) return [];
		throw error;
	}
	if (walkInfo.isFile()) {
		const relativePath = toPosixRelative(from, walkRoot);
		if (!matcher.test(relativePath)) return [];
		return [relativePath];
	}
	if (!walkInfo.isDirectory()) return [];
	const names = await (0, node_fs_promises.readdir)(walkRoot, { recursive: true });
	const matches = [];
	for (const name of names) {
		const absolutePath = (0, node_path.join)(walkRoot, name);
		if (!isInsideRoot(from, absolutePath)) throw new Error(`copy_to_workspace: matched path escapes source root: ${absolutePath}`);
		const info = await (0, node_fs_promises.stat)(absolutePath);
		const relativePath = toPosixRelative(from, absolutePath);
		if (!info.isFile() || !matcher.test(relativePath)) continue;
		matches.push(relativePath);
	}
	return matches;
}
function globStaticPrefix(glob) {
	const segments = [];
	for (const segment of glob.split("/")) {
		if (segment === "" || segment.includes("*") || segment.includes("?")) break;
		segments.push(segment);
	}
	return segments;
}
function toPosixRelative(from, absolutePath) {
	return (0, node_path.relative)(from, absolutePath).split(node_path.sep).join("/");
}
function isInsideRootOrEqual(root, absolutePath) {
	return (0, node_path.resolve)(root) === (0, node_path.resolve)(absolutePath) || isInsideRoot(root, absolutePath);
}
function isInsideRoot(root, absolutePath) {
	const rel = (0, node_path.relative)((0, node_path.resolve)(root), (0, node_path.resolve)(absolutePath));
	return rel !== "" && !rel.startsWith(`..${node_path.sep}`) && rel !== ".." && !rel.startsWith("../");
}
function assertSafeRelative(relativePath, label) {
	const segments = relativePath.split("/");
	if (segments.length === 0 || segments.some((segment) => segment === "" || segment === "." || segment === "..")) throw new Error(`copy_to_workspace: ${label} is not a safe relative path: ${relativePath}`);
}
function stripBase(relativePath, base) {
	const pathSegments = relativePath.split("/");
	const baseSegments = base.replaceAll("\\", "/").split("/").filter((segment) => segment !== "");
	if (!matchesBasePrefix(pathSegments, baseSegments)) throw new Error(`copy_to_workspace: path "${relativePath}" does not match base "${base}"`);
	const destinationSegments = pathSegments.slice(baseSegments.length);
	if (destinationSegments.length === 0 || destinationSegments.some((segment) => segment === "" || segment === "..")) throw new Error(`copy_to_workspace: stripping base "${base}" from "${relativePath}" left an empty destination`);
	return destinationSegments.join("/");
}
function matchesBasePrefix(pathSegments, baseSegments) {
	if (pathSegments.length <= baseSegments.length) return false;
	for (const [index, baseSegment] of baseSegments.entries()) {
		const pathSegment = pathSegments[index];
		if (pathSegment === void 0) return false;
		if (baseSegment !== "*" && baseSegment !== pathSegment) return false;
	}
	return true;
}
function globToRegExp(glob) {
	const pattern = glob.replaceAll("\\", "/");
	let regex = "^";
	let index = 0;
	while (index < pattern.length) {
		const char = pattern[index];
		if (char === void 0) break;
		if (char === "*" && pattern[index + 1] === "*") {
			if (pattern[index + 2] === "/") {
				regex += "(?:.*/)?";
				index += 3;
				continue;
			}
			regex += ".*";
			index += 2;
			continue;
		}
		if (char === "*") {
			regex += "[^/]*";
			index += 1;
			continue;
		}
		if (char === "?") {
			regex += "[^/]";
			index += 1;
			continue;
		}
		regex += escapeRegExp(char);
		index += 1;
	}
	return new RegExp(`${regex}$`);
}
function escapeRegExp(char) {
	return char.replaceAll(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function isEnoent(error) {
	return typeof error === "object" && error !== null && "code" in error && error.code === "ENOENT";
}
//#endregion
//#region src/given/the_prompt.ts
function the_prompt(prompt) {
	return function() {
		this.prompt = prompt;
	};
}
//#endregion
//#region src/when/executing_the_agent.ts
async function executing_the_agent() {
	if (this.workspace === void 0 || this.workspace === "") throw new Error("executing_the_agent requires this.workspace; use a_workspace in given");
	if (this.prompt === void 0 || this.prompt === "") throw new Error("executing_the_agent requires this.prompt; use the_prompt(...) in given");
	if (this.agent === void 0) throw new Error("executing_the_agent requires this.agent; use agent({ name, model }) in withAspect");
	this.agentResult = await this.agent.run({
		workspace: this.workspace,
		prompt: this.prompt,
		image: this.image,
		...this.model !== void 0 ? { model: this.model } : {}
	});
}
//#endregion
//#region src/given/agent.ts
function agent(options) {
	return async function() {
		const registryOptions = options.packageRoot !== void 0 ? { packageRoot: options.packageRoot } : {};
		const resolved = new clanker_cleanroom.Agent(options.name, registryOptions);
		this.agent = resolved;
		this.image = options.image ?? resolved.image;
		if (options.model !== void 0) this.model = options.model;
		await (0, clanker_cleanroom.ensureDockerImage)(this.image);
	};
}
//#endregion
exports.AGENTS_GWT_TMP_ROOT = AGENTS_GWT_TMP_ROOT;
Object.defineProperty(exports, "Agent", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.Agent;
	}
});
Object.defineProperty(exports, "AgentFs", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.AgentFs;
	}
});
Object.defineProperty(exports, "BASE_IMAGE", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.BASE_IMAGE;
	}
});
Object.defineProperty(exports, "CONTAINER_HOME", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.CONTAINER_HOME;
	}
});
Object.defineProperty(exports, "CONTAINER_INPUT", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.CONTAINER_INPUT;
	}
});
Object.defineProperty(exports, "CONTAINER_OUTPUT", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.CONTAINER_OUTPUT;
	}
});
Object.defineProperty(exports, "CONTAINER_TRAJECTORY_PATH", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.CONTAINER_TRAJECTORY_PATH;
	}
});
Object.defineProperty(exports, "CONTAINER_WORKSPACE", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.CONTAINER_WORKSPACE;
	}
});
Object.defineProperty(exports, "PACKAGE_ROOT", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.PACKAGE_ROOT;
	}
});
Object.defineProperty(exports, "TRAJECTORY_FILE", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.TRAJECTORY_FILE;
	}
});
exports.a_workspace = a_workspace;
exports.agent = agent;
Object.defineProperty(exports, "asRecord", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.asRecord;
	}
});
Object.defineProperty(exports, "bindingRegistry", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.bindingRegistry;
	}
});
Object.defineProperty(exports, "buildDockerRunArgs", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.buildDockerRunArgs;
	}
});
Object.defineProperty(exports, "buildImages", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.buildImages;
	}
});
exports.cleanup_workspace = cleanup_workspace;
exports.copy_to_workspace = copy_to_workspace;
Object.defineProperty(exports, "createAgent", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.createAgent;
	}
});
Object.defineProperty(exports, "ensureDockerImage", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.ensureDockerImage;
	}
});
exports.executing_the_agent = executing_the_agent;
Object.defineProperty(exports, "extractTextContent", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.extractTextContent;
	}
});
Object.defineProperty(exports, "invokeDocker", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.invokeDocker;
	}
});
Object.defineProperty(exports, "parseAgentJsonOutput", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.parseAgentJsonOutput;
	}
});
Object.defineProperty(exports, "parseTrajectory", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.parseTrajectory;
	}
});
Object.defineProperty(exports, "readRegistry", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.readRegistry;
	}
});
Object.defineProperty(exports, "readString", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.readString;
	}
});
Object.defineProperty(exports, "readTrajectory", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.readTrajectory;
	}
});
Object.defineProperty(exports, "registerBinding", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.registerBinding;
	}
});
Object.defineProperty(exports, "resetBindings", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.resetBindings;
	}
});
Object.defineProperty(exports, "resetBuildMemo", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.resetBuildMemo;
	}
});
Object.defineProperty(exports, "resetRegistry", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.resetRegistry;
	}
});
Object.defineProperty(exports, "resolveBinding", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.resolveBinding;
	}
});
Object.defineProperty(exports, "resolveImage", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.resolveImage;
	}
});
Object.defineProperty(exports, "runBoundAgent", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.runBoundAgent;
	}
});
Object.defineProperty(exports, "runDocker", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.runDocker;
	}
});
exports.the_prompt = the_prompt;
Object.defineProperty(exports, "unregisterBinding", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.unregisterBinding;
	}
});
Object.defineProperty(exports, "upsertRegistryEntry", {
	enumerable: true,
	get: function() {
		return clanker_cleanroom.upsertRegistryEntry;
	}
});

//# sourceMappingURL=index.cjs.map