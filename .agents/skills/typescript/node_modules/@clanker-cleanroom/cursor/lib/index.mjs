import { a as defaultHostAuthFile, i as CURSOR_IMAGE, n as adaptCursorEvents, r as CONTAINER_AUTH_PATH, t as cursorBinding } from "./binding-FbfJgSVd.mjs";
import { Agent } from "clanker-cleanroom";
import { join } from "node:path";
import { fileURLToPath } from "node:url";
//#region src/agent.ts
const cursorAgent = Agent.fromBinding(cursorBinding);
//#endregion
//#region src/package-root.ts
/** Absolute path to this package root (parent of `src/` or `lib/`). */
const PACKAGE_ROOT = fileURLToPath(new URL("..", import.meta.url));
/** Folder of `*.Dockerfile` files for this agent. */
const DOCKER_DIR = join(PACKAGE_ROOT, "docker");
//#endregion
export { CONTAINER_AUTH_PATH, CURSOR_IMAGE, DOCKER_DIR, PACKAGE_ROOT, adaptCursorEvents, cursorAgent, cursorBinding, defaultHostAuthFile };

//# sourceMappingURL=index.mjs.map