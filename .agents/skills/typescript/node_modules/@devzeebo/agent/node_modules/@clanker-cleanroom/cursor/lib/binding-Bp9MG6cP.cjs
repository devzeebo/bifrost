let clanker_cleanroom = require("clanker-cleanroom");
let node_fs_promises = require("node:fs/promises");
let node_fs = require("node:fs");
let node_os = require("node:os");
//#region src/constants.ts
const CURSOR_IMAGE = "clanker-cleanroom/cursor";
const CONTAINER_AUTH_PATH = `${clanker_cleanroom.CONTAINER_HOME}/.config/cursor/auth.json`;
const defaultHostAuthFile = (home) => `${home}/.config/cursor/auth.json`;
//#endregion
//#region src/trajectory.ts
function adaptCursorEvents(rawEvents) {
	const events = [];
	for (const raw of rawEvents) {
		const record = (0, clanker_cleanroom.asRecord)(raw);
		if (record === void 0) {
			events.push({
				kind: "other",
				type: "unknown",
				raw
			});
			continue;
		}
		const type = (0, clanker_cleanroom.readString)(record.type) ?? "unknown";
		switch (type) {
			case "system":
				events.push({
					kind: "system",
					sessionId: (0, clanker_cleanroom.readString)(record.session_id),
					model: (0, clanker_cleanroom.readString)(record.model),
					raw
				});
				break;
			case "user":
				events.push({
					kind: "user",
					text: (0, clanker_cleanroom.extractTextContent)(record.message),
					raw
				});
				break;
			case "assistant":
				events.push({
					kind: "assistant",
					text: (0, clanker_cleanroom.extractTextContent)(record.message),
					raw
				});
				break;
			case "tool_call":
				events.push(adaptCursorToolCall(record, raw));
				break;
			case "result":
				events.push({
					kind: "result",
					text: (0, clanker_cleanroom.readString)(record.result) ?? "",
					isError: record.is_error === true,
					durationMs: (0, clanker_cleanroom.readTokenCount)(record.duration_ms),
					costUsd: (0, clanker_cleanroom.readTokenCount)(record.total_cost_usd),
					raw
				});
				break;
			default: events.push({
				kind: "other",
				type,
				raw
			});
		}
	}
	return events;
}
function adaptCursorToolCall(record, raw) {
	const phase = (0, clanker_cleanroom.readString)(record.subtype) === "completed" ? "completed" : "started";
	const toolCall = (0, clanker_cleanroom.asRecord)(record.tool_call) ?? {};
	const name = Object.keys(toolCall)[0] ?? "unknown";
	const details = (0, clanker_cleanroom.asRecord)(toolCall[name]) ?? {};
	return {
		kind: "tool",
		phase,
		callId: (0, clanker_cleanroom.readString)(record.call_id) ?? "",
		name,
		args: details.args ?? null,
		result: details.result ?? null,
		raw
	};
}
//#endregion
//#region src/binding.ts
const cursorBinding = {
	image: CURSOR_IMAGE,
	displayName: "Cursor",
	trajectoryKind: "cursor",
	adaptEvents: adaptCursorEvents,
	command: ({ prompt, model }) => {
		const agentArgs = [
			"agent",
			"-p",
			"--force",
			"--output-format",
			"stream-json"
		];
		if (model !== void 0 && model !== "") agentArgs.push("--model", model);
		agentArgs.push("--", prompt);
		return agentArgs;
	},
	prepare: async () => {
		const authFile = defaultHostAuthFile((0, node_os.homedir)());
		try {
			await (0, node_fs_promises.access)(authFile, node_fs.constants.R_OK);
		} catch {
			throw new Error(`Cursor credentials not found at ${authFile}. Run \`agent login\` on the host first.`);
		}
		return { volumes: [{
			host: authFile,
			container: CONTAINER_AUTH_PATH,
			mode: "ro"
		}] };
	},
	parseResult: parseCursorResult
};
function parseCursorResult(trajectory) {
	const parsed = (0, clanker_cleanroom.parseAgentJsonOutput)(trajectory);
	const record = (0, clanker_cleanroom.asRecord)(parsed);
	const usage = (0, clanker_cleanroom.asRecord)(record?.usage);
	return {
		durationMs: (0, clanker_cleanroom.readTokenCount)(record?.duration_ms),
		costUsd: null,
		usage: {
			inputTokens: (0, clanker_cleanroom.readTokenCount)(usage?.inputTokens),
			outputTokens: (0, clanker_cleanroom.readTokenCount)(usage?.outputTokens),
			cacheReadTokens: (0, clanker_cleanroom.readTokenCount)(usage?.cacheReadTokens),
			cacheWriteTokens: (0, clanker_cleanroom.readTokenCount)(usage?.cacheWriteTokens)
		}
	};
}
//#endregion
Object.defineProperty(exports, "CONTAINER_AUTH_PATH", {
	enumerable: true,
	get: function() {
		return CONTAINER_AUTH_PATH;
	}
});
Object.defineProperty(exports, "CURSOR_IMAGE", {
	enumerable: true,
	get: function() {
		return CURSOR_IMAGE;
	}
});
Object.defineProperty(exports, "adaptCursorEvents", {
	enumerable: true,
	get: function() {
		return adaptCursorEvents;
	}
});
Object.defineProperty(exports, "cursorBinding", {
	enumerable: true,
	get: function() {
		return cursorBinding;
	}
});
Object.defineProperty(exports, "defaultHostAuthFile", {
	enumerable: true,
	get: function() {
		return defaultHostAuthFile;
	}
});

//# sourceMappingURL=binding-Bp9MG6cP.cjs.map