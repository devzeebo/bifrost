{
  "name": "bifrost",
  "private": "true",
  "type": "module",
  "devDependencies": {
    "oxlint": "^1.63.0",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3"
  },
  "workspaces": [
    "orchestrator",
    "bifrost/ui"
  ],
  "scripts": {
    "lint:all": "npm run --workspaces lint --if-present",
    "lint": "oxlint",
    "format": "npm run --workspaces format --if-present"
  }
}
