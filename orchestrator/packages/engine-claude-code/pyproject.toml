[project]
name = "engine-claude-code"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "interface-engine",
    "claude-agent-sdk",
    "watchdog",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/engine_claude_code"]
